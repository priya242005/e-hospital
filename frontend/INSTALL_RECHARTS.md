# Install Recharts for Charts

Run this command in the frontend directory:

```bash
cd frontend/e-hospital-dashboard
npm install recharts
```

Or if using yarn:

```bash
yarn add recharts
```

This will install Recharts library needed for the Hospital Dashboard charts.
